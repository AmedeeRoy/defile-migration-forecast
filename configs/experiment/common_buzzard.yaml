# @package _global_

# to execute this experiment run:

defaults:
  - override /data: defile
  - override /model: unet

data :
  species: "Common Buzzard"
  # Buse variable

model:
  optimizer:
    _target_: torch.optim.Adam
    _partial_: true
    lr: 0.02644218004857521
    weight_decay: 0.006036431232167953

  criterion:
    tweedie:
      _target_: src.models.criterion.TweedieLoss
      alpha: 8.287946511017436
      p: 1.3504277144335508
    rmse:
      _target_: src.models.criterion.ProbaRMSE
      alpha: 1