# @package _global_

# to execute this experiment run:

defaults:
  - override /data: defile
  - override /model: unet

data :
  species: "European Honey-buzzard"
  # Bondrée apivore

model:
  optimizer:
    _target_: torch.optim.Adam
    _partial_: true
    lr: 0.01586545404831044
    weight_decay: 0.02999222709845246

  criterion:
    tweedie:
      _target_: src.models.criterion.TweedieLoss
      alpha: 3.6281048434333463
      p: 1.1972628281681463
    rmse:
      _target_: src.models.criterion.ProbaRMSE
      alpha: 1