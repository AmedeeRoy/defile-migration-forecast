# @package _global_

defaults:
  - _self_
  - data: defile
  - model: default
  - logger: null
  - trainer: default
  - export: default
  - paths: default
  - extras: default
  - hydra: default

task_name: "eval"

tags: ["dev"]

# passing checkpoint path is necessary for evaluation
ckpt_path: ${paths.log_dir}/train/runs/2024-07-10_13-06-57/checkpoints/epoch_004.ckpt
